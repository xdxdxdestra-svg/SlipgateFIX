<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
    <!--
      ВАЖНО: этот файл используется и как `entitlements`, и как
      `entitlementsInherit` (см. electron-builder.yml), т.е. применяется ко всем
      вложенным бинарникам внутри Contents/ — в частности к
      resources/tgws/TgWsProxy (PyInstaller onefile).
      @electron/osx-sign подписывает каждый бинарник с hardened runtime
      (electron-builder передаёт hardening для КАЖДОГО файла), поэтому без
      этого entitlement у TgWsProxy включается library validation и встроенный
      в архив Python.framework (подписан другим Team ID) не загружается:
        [PYI-...] Failed to load Python shared library ... different Team IDs
    -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
  </dict>
</plist>
